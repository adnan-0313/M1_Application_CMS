#include "unity.h"
#include <function.h>



#define PROJECT_NAME    "contact_management_system"

/* Prototypes for all the test functions */
void test_add_contact(void);


/* Required by the unity test framework */
void setUp(){}
/* Required by the unity test framework */
void tearDown(){}

/* Start of the application test */
int main()
{
/* Initiate the Unity Test Framework */
  UNITY_BEGIN();

/* Run Test functions */
  RUN_TEST(add_contact);


  /* Close the Unity Test Framework */
  return UNITY_END();
}

/* Write all the test functions */ 
void test_add_contact(void) {
  TEST_ASSERT_EQUAL(1, open(name.contact));
  TEST_ASSERT_EQUAL(2, open(mobile no.contact));
  TEST_ASSERT_EQUAL(3, open(email.contact));
  TEST_ASSERT_EQUAL(4, open(address.contact));
 
    /* Dummy fail*/
  // TEST_ASSERT_EQUAL(0, file does not exist);
  
}
